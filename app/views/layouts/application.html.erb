<!DOCTYPE html>
<html>
  <head>
    <title>Huttsu no Blog</title>
    <%= favicon_link_tag('favicon.png') %>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!-- google font -->
    <link href="https://fonts.googleapis.com/css?family=Kosugi+Maru|Sawarabi+Gothic" rel="stylesheet">
    <!-- Realtime preview -->
    <script src='https://cdnjs.cloudflare.com/ajax/libs/vue/1.0.10/vue.js'></script>
    <!-- Mark down -->
    <script src='https://cdnjs.cloudflare.com/ajax/libs/marked/0.3.5/marked.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/uikit/3.0.3/css/uikit-core-rtl.css'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/core.js'></script>
    <script src='https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.43.0/addon/comment/comment.js'></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/uikit/2.27.5/css/components/htmleditor.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/uikit/2.27.5/js/components/htmleditor.js"></script>
  </head>

  <body>
    <div class="html container-fluid">
      <%= render 'header' %>
      <div class="center-block">
        <!-- flash toastrの設定 -->
        <% flash.each do |type, msg| %>
          <% type = 'success' if type == 'success' %>
          <% type = 'error' if type == 'danger' %>
          <% type = 'warning' if type == 'alert' %>
          <script>
            toastr['<%= type %>']('<%= msg %>');
          </script>
        <% end %>
      </div>
      <%= yield %>
      <!-- 開発環境でのデバック -->
      <%= debug(params) if Rails.env.development? %>
      <!-- フッター -->
      <footer class="blog-footer text-center mt-5">
       <p><%= link_to 'このブログについて', "#" %></p>
       <p>©️ 2019 Huttsu no Blog</p>
       </footer>
       <!-- フレーム -->
        <div id="websiteFrameTop" class="websiteFrame"></div>
        <div id="websiteFrameRight" class="websiteFrame"></div>
        <div id="websiteFrameBottom" class="websiteFrame"></div>
        <div id="websiteFrameLeft" class="websiteFrame"></div>
    </div>
  </body>
</html>
